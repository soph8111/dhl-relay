{
  "name": "dhl-relay",
  "private": true,
  "workspaces": [
    "client",
    "runner-app",
    "server",
    "packages/*"
  ],
  "scripts": {
    "dev": "concurrently \"npm run sanity\" \"npm run client\" \"npm run runner-app\" \"npm run server\""
  },
  "devDependencies": {
    "concurrently": "^10.0.5"
  }
}
